{% from "./otherInput.njk" import otherInput %}

    {% set otherTextInput %}
        {{ otherInput('TUSED-otherInput', discrepancy.causes.TUSED.other) }}
    {% endset -%}

    {{ govukRadios({
        classes: "govuk-radios--small",
        name: "TUSED-subType",
        errorMessage: validationErrors | findError('TUSED-subType'),
        items: [
            {
                value: "DATE_NOT_CALCULATED",
                text: "Date not calculated",
                checked: discrepancy.causes.TUSED.subCategory == 'DATE_NOT_CALCULATED'
            },
            {
                value: "OTHER",
                text: "Other",
                checked: discrepancy.causes.TUSED.subCategory == 'OTHER',
                conditional: {
                    html: otherTextInput
                }
            }
        ]
    }) }}

