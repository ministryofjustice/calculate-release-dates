{% from "./otherInput.njk" import otherInput %}

    {% set otherTextInput %}
        {{ otherInput('PED-otherInput', discrepancy.causes.PED.other) }}
    {% endset -%}

    {{ govukRadios({
        classes: "govuk-radios--small",
        name: "PED-subType",
        errorMessage: validationErrors | findError('PED-subType'),
        items: [
            {
                value: "DATE_NOT_CALCULATED",
                text: "Date not calculated",
                checked: discrepancy.causes.PED.subCategory == 'DATE_NOT_CALCULATED'
            },
            {
                value: "OTHER",
                text: "Other",
                checked: discrepancy.causes.PED.subCategory == 'OTHER',
                conditional: {
                    html: otherTextInput
                }
            }
        ]
    }) }}

